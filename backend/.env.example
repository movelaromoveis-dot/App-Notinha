# Frontend
VITE_API_URL=http://localhost:4001

# Backend
PORT=4000
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=your_password_here
DB_NAME=notafacil
JWT_SECRET=your_jwt_secret_here_change_in_production
JWT_EXPIRES=8h

# Docker (when using docker-compose)
# DB_HOST=db (when running in Docker network)
